import 'package:flutter/material.dart';

class ListadoCasasPage extends StatelessWidget {
  const ListadoCasasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFFF229A8),
        title: Text('Listado'),
        centerTitle: true,
      ),
    );
  }
}
